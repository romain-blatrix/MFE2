import React from "react";

const App = () => (
  <div>
    <h1>MMP</h1>
    {/* {Object.entries(routes).map(([domain, routes]) => (
      <div>
        <h2>{domain}</h2>
        <ul>
          {routes.map(({path, label}) => <li>{label}</li>)}
        </ul>
      </div>
    ))} */}
  </div>
);

export default App;
